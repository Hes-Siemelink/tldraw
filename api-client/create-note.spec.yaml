Input parameters:
  room:
    default: my-room
  text:
    default: ""
  x:
    default: 120
  y:
    default: 250

Connect to: TLDraw

POST: 
  path: /${room}/shapes
  body: 
    x: ${x}
    y: ${y}
    type: note    
    text: ${text}

On error:
  Print: ${error}